<h1>OpenERP Server Detail</h1>
<table>
  <tr>
    <th>Database</th>
    <th>Username</th>
    <th>Password</th>
    <th>Url</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @erps.each do |erp| %>
  <tr>
    <td><%= erp.database %></td>
    <td><%= erp.username %></td>
    <td><%= erp.password %></td>
    <td><%= erp.url %></td>
    <td><%= link_to 'Show', erp %></td>
    <td><%= link_to 'Edit', edit_erp_path(erp) %></td>
    <td><%= link_to 'Destroy', erp, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New OpenERP Server Details', new_erp_path %>
